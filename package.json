{
  "name": "macau-sync-v2",
  "version": "1.0.0",
  "description": "澳门洗码报表 v10.21 正式版 — 全新部署",
  "main": "server.js",
  "scripts": {
    "start": "node build.js && node server.js",
    "build": "node build.js",
    "test": "node test.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
